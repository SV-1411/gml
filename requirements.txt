# GML Infrastructure - Production Dependencies
# Simplified for Vercel + Render + Supabase + Pinecone

# Web Framework
fastapi==0.115.4
uvicorn[standard]==0.32.1

# Data Validation
pydantic==2.9.2
pydantic-settings==2.5.0

# Database - SQLAlchemy (ONLY for Alembic migrations)
# Primary database access is via Supabase client
sqlalchemy>=2.0.40  # Required for Alembic migrations only
asyncpg==0.30.0  # Required for Alembic migrations only

# Supabase Client (PRIMARY database access)
supabase==2.7.4

# Database migrations (uses SQLAlchemy)
alembic==1.16.5

# Vector Database - Pinecone (managed, no infrastructure)
pinecone-client==5.0.0

# Caching - Redis (optional)
redis==5.2.0

# HTTP Client
httpx==0.27.0

# Security & JWT
cryptography==43.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Monitoring
prometheus-client==0.21.0

# Configuration & Environment
python-dotenv==1.0.1
pyyaml==6.0.2

# Utilities
python-dateutil==2.8.2
pytz==2024.1

# AI/ML Services
openai>=1.0.0

# Form Data Support (Required for file uploads)
python-multipart>=0.0.6
